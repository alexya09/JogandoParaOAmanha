/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
/*
horizontal_movement = 0;
vertical_movement = 0;

src_collision(o_PushPull);

x+= horizontal_movement;
y+= vertical_movement;

var collision = noone;
interaction_radius = (sprite_width / 2);
collision = collision_rectangle(x - interaction_radius,y - interaction_radius, x+interaction_radius, y+interaction_radius,camila_player,false,true);

if(collision =  noone)
{
	horizontal_movement = 0;
	vertical_movement = 0;
} */